export const metadata = {
  title: "Test Page | Word Experts",
  description: "Testing purposes only.",

  robots: {
    follow: false,
    index: false,
  },
};

export default function Layout({ children }) {
  return <>{children}</>;
}
